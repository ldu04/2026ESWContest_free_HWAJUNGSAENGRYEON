# 실측값 대장 — 이 프로젝트의 숫자 정본 (2026-09-01)

> **왜 이 문서가 있나**: 같은 값이 여러 문서에 흩어져 있으면 **언젠가 어긋난다.**
> 실제로 어긋났다 — `D1_리허설_절차서.md` §4-6 과 `파이_준비_절차.md` 의
> 「시작 로그 기대값」이 **D-069 시절 값(v 0.00061)** 그대로 남아 있었다(2026-09-01 정정).
> 그 문서는 **런 시작 전에 눈으로 대조하는 검산표**라, 틀린 채로 두면 **맞는 런을 틀렸다고
> 판정한다.** 검산이 반대로 작동한다.
>
> **규칙 세 개**
> 1. 값을 고치면 **여기를 먼저 고친다.** 다른 문서는 여기를 가리킨다.
> 2. 각 행에 **출처(파일·명령)** 를 적는다. 출처 없는 값은 「미측정」이라고 쓴다.
> 3. `python tools/check_values.py` 가 옛 값이 되살아났는지 훑는다. 런 전 점검에 포함돼 있다.

---

## 1. 동결값 — 헌장 §1. **사람만 바꿀 수 있다**

| 값 | 정본 | 출처 |
|---|---|---|
| 사망 임계 | **80.0 ℃** | `firmware/node/config.h` `TEMP_THRESHOLD_C` |
| 경고 임계 | **60.0 ℃** | `firmware/node/config.h` `WARN_TEMP_C` |
| 격자 간격 | **0.20 m** | `gateway/deploy_config.json` `spacing_m` |
| 격자 | **4 × 4 (16노드)** | 〃 `grid_rows`/`grid_cols` |

---

## 2. 대본 — **D-075(2026-09-01 텀 압축)가 정본.** D-074·D-069 값은 옛것이다

| 값 | **정본(D-075)** | 옛값(D-074) | 옛값(D-069) | 출처 |
|---|---|---|---|---|
| 체류시간 | **21 초** | 21 초 | 18 초 | `round(1.7 × t80중앙값)`, 상한 22초 |
| 화선 속도 `v_front_expected` | **0.0005785 m/s** | 0.000579 / 0.000523 | 0.00061 | `gateway/deploy_config.json` |
| `dt_window` (= radio/v) | **622.3 s** | 621.8 / 688.3 s | 590.2 s | 게이트웨이가 실행 시 유도 |
| `alert_horizon` (= spacing/v) | **345.7 s** | 345.4 / 382.4 s | 327.9 s | 〃 |
| `residual_gate_s` | **165.1 s ★실측** | 62.5 / 62.4 / 69.1 s | 36.4 / 59.3 / 20.4 | 아래 참조 |
| 총 런 | **23:20 (1400 s)** | 23:19 / 25:48 (1548 s) | 22:07 / 12:12 | `scripts/route_table.py --solve 7` |
| `n07 → n09` 이동 여유 | **7.0 초** (45.1 cm) | 10.0 초 | 14 / 5.6 초 | 동선표에서 가장 빡빡한 구간 |

> **★ D-075 를 왜 만들었나 (2026-09-01)** — 무응답 추적으로 시간이 밀리면서 uptime 여유가
> 애매해졌다. 이동 텀을 줄여 런을 압축했다: 25:48 → **23:20**(−2:28), 총 uptime 32:30 → 30:00.
> 이동여유 5초까지 밀어보려 했으나 `route_table.py` 가 `n07→n09` 를 **「이동 실측 7초보다
> 짧다. 실행 불가」** 로 자체 기각했다. 그래서 그 실측 floor 인 **7초에 정확히 맞췄다** —
> **이 구간은 여유가 0초다.** 사전 연습 필수.

> **★ `residual_gate_s` 165.1 은 2026-09-01 실물 런에서 재도출했다** (median 73.50 + 3σ 30.52).
> 출처 `results/hw/run_205330_deaths.csv` · 채택사망 15건 중 잔차 표본 **8건**.
> 이전 값(69.1 계열)보다 훨씬 큰 이유는 **전선 기하가 다르기 때문**이다 — 69.1 은 **직선**
> 전선 드라이런(잔차 median 0.35 s), 촬영 대본 B 는 **방사형**이라 국소 평면 적합에
> **곡률 오차가 구조적으로** 생긴다. 측정이 나빠진 것이 아니다.
> **★ 한계 — 반드시 함께 인용할 것:** 표본 **8건 · 1회차**이고, 이번 런에서 이 문턱은
> **한 번도 쓰이지 않았다**(15건이 branch1_last_gasp, n03 은 표본부족). **실전 미검증**이다.

> `residual_gate_s` 는 **식이 아니라 분포**에서 나온다. v 를 바꿔도 **자동으로 안 따라온다** —
> `scripts/derive_scale_constants.py` 를 다시 돌려야 한다. 값이 5개나 돌아다니는 이유다.

## 3. t80 (사망까지 걸리는 시간) — D-074

| | |
|---|---|
| 측정 대상 | **n07** 의 자기 시리얼 `TT` 트레이스 (무선은 도착률이 낮아 80℃ 통과를 놓친다) |
| 회차 | 12.41 s / **12.49 s** / 17.39 s |
| 채택 | **중앙값 12.49 s** → 체류 `round(1.7 × 12.49) = 21` 초 |
| σ | **미확정** — 3회 σ 2.33 s 이지만 사실상 3회차 하나가 만든 값(1·2회차만 σ 0.04 s) |
| 조건 | 열풍기 **5분 예열**. 조건을 바꾸면 t80 이 달라져 대본이 통째로 밀린다 |
| 원자료 | `results/hw/t80_calib_curve.csv` (1,220 샘플) |

## 4. 좌표

| | |
|---|---|
| `measured` | **true** (실측) |
| 좌표 지문 | `SHA-256(앞16) = ba8dd13f4150c69c` — 노트북·파이 **일치 확인** |
| 최근접거리 중앙값 | **0.199 m** (명목 0.200) |
| 이웃수 분포 | `{3:4, 5:8, 8:4}` — 4×4 정격자 기대값과 일치 |
| 라벨 규약 | `nXX → id = XX−1` (n01→id0 … n16→id15) |
| 방향 참값 | A **75.96°** / B **55.48°** (D-073, 실측 좌표 기준) |
| 점화점(대본 B) | (0.02, −0.11) m · 시스템 프레임 |

## 5. 하드웨어 · 펌웨어 주기

| 값 | 정본 | 출처 |
|---|---|---|
| 하트비트 주기 | **1000 ms (1 Hz)** | `config.h:88` `HEARTBEAT_MS` |
| 침묵 판정 | **3000 ms** | 〃 `SILENCE_TIMEOUT_MS` (하트비트 3회분) |
| 임종신호 지연 | **300 ms** | 〃 `LAST_GASP_DELAY_MS` |
| 온도 샘플 주기 | **500 ms** | 〃 `SENSE_MS` |
| DS18B20 해상도 | **9비트** (변환 93.75 ms) | `node.ino:283`. 12비트면 750 ms 블로킹 |
| LED 핀 | **GPIO5** — **n04 만 GPIO18** | D-072 |
| 노드 수 | 16 + 브리지 99 = **17대** | `results/hw/burn_map.csv` |

> ★ **하트비트는 1초다.** 2026-09-01 에 이걸 5초로 착각해 도착률을 5배 부풀린 적이 있다.

## 6. ★ 메시 전달 성능 — 2026-09-01 소크 실측 (**여기가 지금 최대 위험**)

원자료: `results/hw/soak_16node_20260901_night_*` · 분석: `docs/n07_사망시험_판정_20260901.md`

| 값 | 실측 | 뜻 |
|---|---|---|
| **노드 HB 도착률** | **5~9%** (15대 평균 7.3%) | 단발 임종신호의 통과 확률이 곧 이 값이다 |
| 브리지 자기 HB 도착률 | **65%** | 무선을 안 거치는데도 35%를 잃는다 |
| 브리지 loop() 주기 | **≈0.5 s** (자기 HB 간격 1.519 s) | 설계는 1.0 s. 루프가 굶고 있다 |
| 브리지가 내보내는 노드 HB | **≈1.3개/초** | 노드 15대는 **15개/초**를 보낸다 |
| 노드별 재등장 주기 | **15~21 초** (ms 간격 중앙값 21.0 s) | |
| **브리지 크래시** | **33분 관측에 2회** — uptime 42.8분(Guru Meditation) · **24.5분(abort)** | **크래시 간격 24.5분 < 런 25:48.** 「uptime 40분 미만」 규칙만으로는 부족하다 |
| 크래시 직전 힙 | **두 번 다 `max_alloc 40,948`** | 부팅 직후는 110,580. 약 40초 만에 급락한다(느린 누수가 아니다) |
| 노드 이탈·재합류 | **33분에 2,397회 / 2,392회** (분당 72회) | 급락 직전 `peers` 가 9→17 로 튄다 — 메시 재구성이 힙을 몰아 쓰는 것으로 보인다(**가설**) |
| `routePackage err=4` (NoMemory) | **60건 / 17분** | 브리지가 메모리 부족으로 노드 패킷을 버린다 |
| 포트 개방 시 배수 | **첫 60초에 7,914줄**, 그 뒤 16분간 **0줄** | 「재생 고장」의 정체. 계속되는 고장이 아니다 |
| 브라운아웃(`E BOD`) | **0건** | 8/31 의 전원 문제는 해결된 것으로 보인다 |

## 6-A. 본편 촬영 추정 성능 — 실측 (2026-09-01, run_205330)

**최종 프레임(t=1954.0 s) 원값에서 확정한 값이다. 표기를 하나로 고정한다.**

| 값 | 정본 | 원값 | 출처 |
|---|---|---|---|
| **방향 추정 오차** | **2.85°** | 추정 58.3293° · 참값 55.4800° · 차 +2.8493° | `results/hw/run_205330.json` est.dir |
| **속도 추정 오차** | **+1.67 %** | 추정 0.000588187 m/s · 참값 0.0005785 | 〃 est.speed |
| 대피 경보 | **13 / 16** 노드가 사망 전 수신 | 리드타임 최소 87 · 중앙 344 · 최대 523 · 평균 351 초 | 〃 est.alerts |
| 사망 | **16 / 16** | 대본 대비 σ 1.24 s(n03 제외) / 10.40 s(포함) | `run_205330_deaths.csv` |

> ★ **한때 「2.82° / +1.6%」로 적은 문서가 있었다.** 이미 반올림된 값(58.3 / 0.000588)을
> 다시 계산해서 생긴 오차다. **정본은 2.85° / +1.67%** 이고, 두 표기가 공존하면 안 된다.

## 6-B. 자가치유(경로 재구성) — 실측

**조건이 다르면 값도 다르다. 두 회차를 별개 행으로 남긴다.**

| 값 | 실측 | 조건 | 출처 |
|---|---|---|---|
| **재구성 시간** | **15.9 초** | **15노드** · 하트비트 10초 · 임계 80℃ · 2026-09-01 · **표본 1** | `mesh_selfheal16_20260901_230200_pull2.log` |
| **HB 재개** | **70~80 초** | 〃 · **±10초 상한**(하트비트 10초 주기) · 14/14 노드 | 〃 |
| 재구성 시간 | 26.0 초 | **4노드** · 하트비트 1초 · 임계 40℃ · 2026-08-30 | `docs/실측_자가치유_20260830.md` (c-2) |
| 재합류 후 응용데이터 | 3.0 초 | 〃 (전원 재인가 기준) | 〃 |

> ★ **8/30 값과 9/01 값을 직접 비교하지 말 것.** 노드 수(4 → 15)·하트비트 주기(1초 → 10초)·
> 임계값(40℃ → 80℃)·급전이 전부 다르다. 특히 **HB 재개 70~80초는 하트비트 주기가 10초라
> 생긴 값**이고, 8/30 의 3.0초는 1초 주기에 전원 재인가 기준이라 **다른 것을 잰 값**이다.

> ★ **표본 1회**다. 그리고 그 시험에서 **n13 은 메시에 없었다** — 「16노드」가 아니라
> **「15노드」** 로 기술해야 한다.

> 시험 조건: 중계 노드 n07(홉3 · 자식 4 · 하위 11개, 트리의 73%)을 허브에서 물리적으로 제거.
> BOD·재부팅·PARSE_FAIL **0건**, 브리지 `ms` 단조 증가 → **재시작이 아닌 재라우팅으로 복구**.

## 7. 시험 결과 — 통과/실패

| 시험 | 결과 | 근거 |
|---|---|---|
| LED 전수 | **16/16 초록** (2026-08-31) | `docs/현장_조립카드.md` |
| 메시 점호 | **16/16 + 브리지** (2026-08-31) | 〃 |
| **n07 단발 사망 시험** | **★ 실패** — 노드는 죽었으나 산출물에 사망 0건 | `docs/n07_사망시험_판정_20260901.md` |
| 음성 큐 ↔ 동선표 | **16행 전부 일치** | `python tools/verify_cue_table.py` |
| 감시견 4종 | **6/6 발동** | `python tools/test_watchdogs.py` |
| cp949 스모크 | 진입점 **46개 전수 통과** | `python tools/smoke_stdio.py` |
| 게이트웨이 회귀 | 산출물 **바이트 단위 동일**(사망 13건) | `dryrun_fw_stream.jsonl` 재생 |

## 8. 재료비 — `docs/cowork/build_bom.py`

| | |
|---|---|
| 소계(부가세 별도) | **406,600 원** |
| 부가세 포함 | **447,260 원** (덱 2장의 「16노드 ≈ 45만 원」과 일치) |
| **노드 1대 부품비** | **12,000 원** = ESP32 8,000 + DS18B20 2,700 + WS2812 600 + USB 700 |

## 9. 미측정 · 미확정 — **여기 있는 것은 「모른다」가 정답이다**

| 항목 | 왜 못 정했나 |
|---|---|
| 사망 시각 산포 σ | 실측 3회뿐이고 사실상 1회차가 만든 값. **리허설 한 번이면 16건이 나온다** |
| 파손 여유(125 ℃ 까지) | 80℃ 통과 직후 열원을 떼서 상승 기울기 표본이 1개뿐. 문서의 「파손 19.8초」는 **임계 60℃ 시절 값** |
| n07 이 105 ℃ 를 견딘 것 | 파손 여유의 **하한 증거는 된다** — 105 ℃ 를 겪고도 지금 실온을 정상 판독한다. 상한은 여전히 미측정 |
| 브리지 loop() 가 왜 0.5초인가 | 펌웨어 계측이 필요 → 재굽기 → 사람 |
| 브리지가 **못 받는지 / 받고도 안 찍는지** | 같은 이유. `DV` 유무로는 못 가른다(투표는 `silent AND hot` 조건) |
| n02 · n03 센서 | 자극(열)을 준 적이 없다. **판정 불가**이지 고장이 아니다 |
| n09 LED | 노드는 메시에서 정상 보고 중 → 배선 의심. 눈으로 봐야 한다 |
| 다리 레인 15/35/55 | 사용자 결정이고 **실측 기록이 없다** |
| 브리지 USB 케이블 불량 | 「재생 고장」이 배수로 설명되므로 **재검토 대상**. 케이블이 원인이라는 근거가 약해졌다 |
